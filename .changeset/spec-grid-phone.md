---
"@stapel/listings-react": patch
---

Characteristics on a phone: the label track is capped at 45% of the list (`fit-content(45%)`) and a value wraps by word (`overflow-wrap: break-word`), so one long label no longer leaves the values a 15px column that breaks per letter. A listing with a single photograph fills the gallery strip instead of leaving the 14% peek blank.
