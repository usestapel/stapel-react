---
"@stapel/shell-react": patch
---

`matchesLocation` matches a multi-segment relative `linkPath` (e.g. `workspaces/settings`) as a whole instead of on its last segment only, so such menu rows now highlight as active.
