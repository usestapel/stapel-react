---
"@stapel/video-react": patch
---

Regenerated against **stapel-video 0.12.0**: the pin was one minor behind
(0.11.2). The declared backend contract moves to `>=0.12 <0.13` in
`manifest.json` and `llms.txt`. `docs/schema.json`, `docs/errors.json` and
`docs/flows.json` are byte-identical across 0.11.2..0.12.0 — a pure
freshness bump, nothing generated in this package moves beyond the version
stamp.
