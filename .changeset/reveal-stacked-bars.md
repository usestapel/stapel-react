---
"@stapel/core": patch
---

`reveal`: bars that stack (a sticky footer on a fixed dock) or float off the edge (a dock with a margin) are followed in from the edge, so a field is no longer left under the upper one.
