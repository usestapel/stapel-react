---
"@stapel/shell-react": patch
---

The settings theme control fits its three named segments on one row on a 360px phone: segment side padding 0.75rem -> 0.4375rem, glyph gap 0.4375rem -> 0.25rem (measured 347 of 352px at 360). The track still wraps on narrower mounts.
