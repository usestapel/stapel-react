---
"@stapel/categories-react": minor
---

`partitionSides(children)`: the sides a partition parent files a listing under — its own row children, never a CategoryLink pointer into another branch and never a value of an expanded branch.
