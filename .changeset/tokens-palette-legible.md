---
"@stapel/tokens": patch
---

Token palette demo: every cell now prints and copies its value (literal hex for the raw ramps, the runtime-resolved colour for the neutral roles so it follows `data-theme`), the scales draw real specimens (a bar of that width, a box with that corner, a glyph at that size), and the grids stay multi-column at 390px — the phone page drops from 19,004px to 4,163px.
