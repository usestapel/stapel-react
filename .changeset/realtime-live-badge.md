---
"@stapel/realtime": patch
---

Replace the demos' raw state dump with `demo/LiveBadge.tsx`, the README's connection surface built for real: a coloured dot, one human sentence per state, a labelled Reconnect button only where retrying can change the answer, and the wire envelope moved into a collapsed developer disclosure.
