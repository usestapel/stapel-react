---
"@stapel/moderation-react": patch
---

Regenerated against **stapel-moderation 0.8.0**: the pin was one minor
behind (0.7.2). The declared backend contract moves to `>=0.8 <0.9` in
`manifest.json` and `llms.txt`. `docs/schema.json`, `docs/errors.json` and
`docs/flows.json` are byte-identical across 0.7.2..0.8.0 — a pure freshness
bump, nothing generated in this package moves beyond the version stamp.
