---
"@stapel/profiles-react": minor
---

Add this pair's nav-manifest entry (`src/nav/manifest.ts`, `profiles.settings` — the top-level entry other pairs' submenu entries nest under, e.g. auth-react's `auth.security`) for the scripted-fullstack navigation contract. New i18n key `profiles.nav.settings` (en + ru).
