# @stapel/search-react 0.21.0

Headless React flow pair for stapel-search (contract >=0.14 <0.15) — business + state, zero visual opinion.
Built on @stapel/core: typed client + StapelApiError envelope, auth token refresh,
verification-403 interception, i18n engine, analytics facade, TanStack Query layer.

## The one right way (do this, the rest is a review/lint smell)
- No raw fetch/axios. The client is injected via <SearchProvider>/StapelConfigProvider;
  every hook and flow already carries auth, refresh, and the error envelope.
- Render errors, never try/catch them: a flow's state carries FlowError{code,params};
  render `t(code, params)` and branch on `explainSearchError(code)` remediation.
- Server state = the use* hooks (query layer); keys come only from searchQueryKeys.
- User strings = i18n keys (registerSearchI18n); never string literals.
- Sign-in UI = a headless flow component; copy it (shadcn-style) to restyle.

## Layers
api (typed client) · model (hooks, session) · flows (machines) · headless · i18n

## Documented flows (flows.json — canonical id, steps, endpoints)

## Operations (typed; use the named op, never a path string)
Request/response schema names are in manifest.json + the generated types.
Paths are relative to `/search/api/v1/`.
- search_api_v1_health_retrieve: GET /health
- search_api_v1_query_retrieve: GET /query
- search_api_v1_ranking_retrieve: GET /ranking
- search_api_v1_reindex_create: POST /reindex
- search_api_v1_suggest_retrieve: GET /suggest

## Query hooks (server state; keys come only from the key factory)
- useRankingDisclosure (query) → ranking
- useSearchQuery (query) → query
- useSuggest (query) → suggest

## Errors (render t(code, params); UX from remediation)
54 keys (full catalog: manifest.json §errors). By remediation: fix_input 26 · retry 15 · verify 5 · wait_and_retry 5 · contact_support 2 · reauthenticate 1.
Param-bearing keys (interpolation slots matter):
- error.400.field.blank [400] → fix_input {field}
- error.400.field.does_not_exist [400] → fix_input {field}
- error.400.field.invalid [400] → fix_input {field}
- error.400.field.invalid_choice [400] → fix_input {field}
- error.400.field.max_length [400] → fix_input {field,max_length}
- error.400.field.max_value [400] → fix_input {field,max_value}
- error.400.field.min_length [400] → fix_input {field,min_length}
- error.400.field.min_value [400] → fix_input {field,min_value}
- error.400.field.null [400] → fix_input {field}
- error.400.field.required [400] → fix_input {field}
- error.400.field.unique [400] → fix_input {field}
- error.400.search_bad_geo [400] → fix_input {reason}
- error.400.search_bad_range [400] → fix_input {slug,reason}
- error.400.search_too_many_facets [400] → fix_input {limit}
- error.400.search_too_many_ranges [400] → fix_input {limit}
- error.400.search_unknown_doc_type [400] → fix_input {doc_type}
- error.400.search_unknown_sort [400] → fix_input {sort}
- error.400.search_window_exceeded [400] → fix_input {window}
- error.429.rate_limit [429] → wait_and_retry {retry_after_minutes}

## Analytics events (typed; defineEvent → events.json, drift-gated)
- (no app defineEvent() in this pair — its analytic events are the
  auto-instrumented flow funnels below)

## Flow funnels (auto-instrumented: flow.<id>.<step> {phase})

```tsx
// Typed event + tracked() click (the one right way; §3.1).
const planSelected = defineEvent({
  name: "pricing.plan.selected",
  description: "User picked a plan",
  props: { plan: prop.oneOf(["free", "pro", "team"], "Plan code") },
});
const { tracked } = useTracked();
<Button onClick={tracked(planSelected, { plan }, startCheckout)} />
// A click that STEPS a flow machine is already instrumented — mark it
// data-analytics="flow" instead; tracked() on top double-counts (§3.2).
```

## Demos (defineDemo → manifest.demos; compiled, linted, rendered examples)
- search.box → <SearchBox> [empty|typed]  demo/SearchBox.demo.tsx
- search.classified-filters → <FilterChips> [chips|applied|shapes|views]  demo/ClassifiedFilters.demo.tsx
- search.degradation-notice → <DegradationNotice> [banner|inline]  demo/DegradationNotice.demo.tsx
- search.empty-exits → <EmptyExits> [every constraint|only the location|with the host's siblings]  demo/EmptyExits.demo.tsx
- search.facet-panel-pane → <FacetPanelPane> [open-search|narrowed|in the desktop rail]  demo/FacetPanelPane.demo.tsx
- search.filter-sheet → <SearchPage> [open]  demo/FilterSheet.demo.tsx
- search.language-select → <LanguageSelect> [offered|from-a-link]  demo/LanguageSelect.demo.tsx
- search.location-summary → <LocationSummaryLine> [everywhere|placed|narrowed|long-place|wide]  demo/LocationSummaryLine.demo.tsx
- search.page → <SearchPage> [desktop|phone|unreadable-link]  demo/SearchPage.demo.tsx
- search.page-size-select → <PageSizeSelect> [ladder|off-ladder]  demo/PageSizeSelect.demo.tsx
- search.range-filter-row → <RangeFilterRow> [untouched|applied]  demo/RangeFilterRow.demo.tsx
- search.ranking-pane → <RankingDisclosurePane> [desktop|phone]  demo/RankingDisclosurePane.demo.tsx
- search.result-card → <SearchResultCard> [gallery|promoted|unresolved|plain]  demo/SearchResultCard.demo.tsx
- search.results-pane → <SearchResultsPane> [desktop|phone|empty]  demo/SearchResultsPane.demo.tsx
- search.sort-select → <SortSelect> [no-centre|with-centre]  demo/SortSelect.demo.tsx
- search.url-issue-notice → <UrlIssueNotice> [two-issues|one-issue]  demo/UrlIssueNotice.demo.tsx
Each source file is the canonical usage snippet (open the default variant).

## Snippets
```tsx
// Error rendering + remediation branch (one pattern for every pair).
const r = explainSearchError(err.code); // 'wait_and_retry' | 'verify' | ...
return <Alert action={r}>{t(err.code, err.params)}</Alert>;
```
