{
  "$generated": "by scripts/gen-demos.mjs — do not edit; drift-gated (pnpm gen:demos:check)",
  "package": "@stapel/tasks-react",
  "version": "0.1.1",
  "demos": [
    {
      "id": "tasks.board-create-sheet",
      "title": "Board create sheet (default skin)",
      "description": "Name plus a shape: the preset list is the server's own vocabulary, and 'custom columns' switches to an explicit column editor whose keys are slugged from the names.",
      "component": "BoardCreateSheet",
      "covers": [],
      "tokens": [],
      "variants": [
        {
          "id": "default",
          "description": "Presets discovered from GET boards/presets, each named by its own columns. A bottom sheet under the tablet breakpoint, a modal above it.",
          "viewport": "phone",
          "step": "idle"
        },
        {
          "id": "scope-blocked",
          "description": "The deployment could not resolve a workspace: the refusal names the action that fixes it.",
          "viewport": "phone",
          "step": "refused"
        }
      ],
      "source": {
        "file": "demo/BoardCreateSheet.demo.tsx",
        "line": 37
      }
    },
    {
      "id": "tasks.boards-pane",
      "title": "Boards (default skin)",
      "description": "The boards list: name, column count, creation date, an archive confirm that is a bottom sheet on a phone, and a create sheet that discovers its presets from the server.",
      "component": "BoardsPane",
      "covers": [],
      "tokens": [],
      "variants": [
        {
          "id": "default",
          "description": "Two boards. The row is one element-width stack, so the same markup is the phone layout — there is no separate phone variant to photograph.",
          "viewport": "phone",
          "step": "ready"
        },
        {
          "id": "empty",
          "description": "No boards yet: the empty arm carries the create button, so the state is not a dead end.",
          "viewport": "phone",
          "step": "empty"
        },
        {
          "id": "failed",
          "description": "The deployment cannot resolve a workspace: a refusal with a retry, never 'you have no boards'.",
          "viewport": "desktop",
          "step": "failed"
        },
        {
          "id": "no-navigation",
          "description": "A host that wired no board navigation: the Open control states the reason beside itself.",
          "viewport": "desktop",
          "step": "gated"
        }
      ],
      "source": {
        "file": "demo/BoardsPane.demo.tsx",
        "line": 37
      }
    },
    {
      "id": "tasks.column-manager",
      "title": "Column manager (default skin)",
      "description": "Drag the columns into order (keyboard sensor included) and add one. Renaming and deleting are not in this API, so the screen explains their absence instead of drawing two controls that cannot work.",
      "component": "ColumnManager",
      "covers": [],
      "tokens": [],
      "variants": [
        {
          "id": "default",
          "description": "Three columns, with thumb-sized drag handles at every width.",
          "viewport": "phone",
          "step": "ready"
        },
        {
          "id": "duplicate",
          "description": "The key is already on the board: a named 409, with the fix beside it.",
          "viewport": "phone",
          "step": "refused"
        }
      ],
      "source": {
        "file": "demo/ColumnManager.demo.tsx",
        "line": 38
      }
    },
    {
      "id": "tasks.kanban-board",
      "title": "Kanban board (default skin)",
      "description": "Columns with WIP counters, cards carrying priority, due date, checklist progress and a blocked glyph, dnd-kit drag with keyboard and touch sensors, and an optimistic move that rolls itself back on a refusal.",
      "component": "KanbanBoard",
      "covers": [
        "TasksProvider",
        "BoardView"
      ],
      "tokens": [],
      "variants": [
        {
          "id": "default",
          "description": "Three columns, six cards. Below the tablet breakpoint the same board becomes one column behind a switcher strip whose chips are drop targets, so the viewer's width control shows it — a duplicate `phone` variant photographed the identical frame.",
          "viewport": "phone",
          "step": "ready"
        },
        {
          "id": "empty",
          "description": "Every column empty — each says so on its own.",
          "viewport": "desktop",
          "step": "empty"
        },
        {
          "id": "truncated",
          "description": "The server's cap cut the board short: the banner says how many are shown and what to do.",
          "viewport": "desktop",
          "step": "truncated"
        }
      ],
      "source": {
        "file": "demo/KanbanBoard.demo.tsx",
        "line": 38
      }
    },
    {
      "id": "tasks.task-sheet",
      "title": "Task sheet (default skin)",
      "description": "One card: title and description saved on blur, column/priority/due controls, assignees through the host seam, a checklist with a three-state overflow menu, and comments where Enter sends and Shift+Enter breaks a line.",
      "component": "TaskSheet",
      "covers": [
        "TaskView"
      ],
      "tokens": [],
      "variants": [
        {
          "id": "default",
          "description": "Editable card — a bottom sheet under the tablet breakpoint, a modal above it. Both come from the same tree, so the viewer's width control is what shows the difference.",
          "viewport": "phone",
          "step": "ready"
        },
        {
          "id": "archived",
          "description": "An archived card: read-only, with one reason stated beside every control.",
          "viewport": "desktop",
          "step": "archived"
        },
        {
          "id": "features",
          "description": "A board whose schema adds custom fields, drawn by the host through the renderFeatures seam — the pair supplies the section and the gate, never the editor.",
          "viewport": "phone",
          "step": "features"
        }
      ],
      "source": {
        "file": "demo/TaskSheet.demo.tsx",
        "line": 100
      }
    }
  ]
}
