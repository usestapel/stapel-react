name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  # Tolerate corepack versions predating the 2025 npm signing-key rotation.
  COREPACK_INTEGRITY_KEYS: "0"

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: write # scripts/auto-bump-safe-pins.mjs pushes a bot commit for wire-identical pins
    steps:
      - uses: actions/checkout@v4
        with:
          # check:peer-floors reads each peer's RELEASE TAGS to learn which
          # version first exported a symbol. The default checkout fetches no
          # tags, which made that gate pass every package unchecked.
          fetch-depth: 0

      - name: Load contract pins (contract-pins.json)
        # §17 pin B: every sibling-backend checkout below is pinned to an
        # immutable ref from contract-pins.json — the single visible place the
        # pins live. Bumping a pin is a deliberate PR after the backend
        # releases a new contract (CONTRIBUTING.md "Contract pins"); never
        # float main. NOTE: the current pins are the siblings' LOCAL HEAD shas
        # (contract commits not pushed/tagged yet) — GitHub can only resolve
        # them once the backend push wave lands; a checkout failure before
        # that is expected and fine.
        id: pins
        run: echo "pins=$(jq -c .modules contract-pins.json)" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable corepack (pnpm)
        run: corepack enable

      - name: Install
        run: corepack pnpm install --frozen-lockfile

      - name: Check out every pinned sibling (contract-pins.json is the list)
        # One loop over contract-pins.json instead of one hand-written
        # checkout step per module: the hand-written list missed six pairs
        # (chat/search/cdn/categories/listings/reviews) and release 0.2.0 of
        # the storefront pairs failed on ENOENT ../stapel-chat/docs/schema.json.
        # stapel-auth lands at .errors-source/ (the env below points there);
        # everything else at .other-source/; all symlinked to ../<module>
        # where the gen:* drivers look. Public repos, shallow clone at the
        # pinned immutable ref — never a floating branch.
        run: |
          set -eu
          for m in $(jq -r '.modules | keys[]' contract-pins.json); do
            ref=$(jq -r ".modules[\"$m\"].ref" contract-pins.json)
            case "$m" in stapel-auth) dest=".errors-source/$m";; *) dest=".other-source/$m";; esac
            git init -q "$dest"
            git -C "$dest" remote add origin "https://github.com/usestapel/$m.git"
            git -C "$dest" fetch -q --depth 1 origin "$ref"
            git -C "$dest" checkout -q FETCH_HEAD
            ln -sfn "$GITHUB_WORKSPACE/$dest" "$GITHUB_WORKSPACE/../$m"
            echo "$m @ $ref"
          done

      - name: Peer floors cover imported symbols
        # A published floor is never exercised in here: every package builds
        # against the workspace core. Only a consumer installing at the floor
        # would find it — after the release. See scripts/check-peer-floors.mjs.
        run: corepack pnpm check:peer-floors

      - name: Auto-bump wire-identical pins (bot commit, no review)
        # scripts/auto-bump-safe-pins.mjs asks the SAME question the gate below
        # asks (does a pin's docs/schema.json / docs/errors.json / docs/flows.json
        # move between the pinned ref and the sibling's newest tag?) and acts on
        # the answer the gate cannot act on itself: when the wire is
        # byte-identical, a pin that is merely behind is not a finding for a
        # human — regenerating against the newer ref provably changes nothing
        # this repo generates. It bumps, regenerates and commits directly;
        # anything wire-MOVED is left exactly alone for the gate below to name
        # and fail on. `pull_request` runs get no write token (and must not push
        # into someone else's branch), so this only runs on `push`.
        #
        # Runs after every sibling is checked out (needs them to diff) and
        # before the freshness gate (so a bump made HERE clears the finding in
        # THIS SAME run, not only the next one) — cheap even when it does
        # nothing: `pnpm gen:pinned` only runs at all when at least one pin
        # qualifies.
        if: github.event_name == 'push'
        run: |
          git config user.name "stapel-bot"
          git config user.email "bot@stapel.dev"
          corepack pnpm run bump:safe-pins
          git push origin "HEAD:${{ github.ref_name }}"

      - name: Contract pin + error-bundle gates
        # `pnpm ci` (package.json) runs check:gates and this workflow did not:
        # the steps below are a hand-copy of that script that drifted, so the
        # pin-freshness half — the one that fails a pin two minors behind its
        # library — has never run on a pull request, only inside `pnpm release`
        # after merge. The freshness check reaches the network (git ls-remote
        # --tags on each sibling's origin, ~8s for 26) because a --depth 1
        # sibling fetch brings down no tags to read locally. Wire-identical pins
        # never reach this check at all on a push run — the step above already
        # bumped them; a wire-MOVED pin is exactly what this still fails on.
        run: corepack pnpm check:gates

      - name: Root scripts' own tests
        run: corepack pnpm run test:scripts

      - name: Codegen drift gates (api / flows / errors / manifest)
        # Regenerates the committed projections from the pinned backend sources
        # and fails if any diverge — types, the flow registry, the backend-error
        # map, and the manifest/llms.txt self-description all under one gate.
        # Sources are per-module: every gen invocation passes its own backend's
        # committed docs/*.json inline (package.json scripts) — the monolith
        # aggregate is retired as a contract source (contract-pipeline.md §5), so
        # its checkout + the API_SCHEMA/FLOWS_JSON env pointing at it are gone.
        # Only the two BARE invocations (auth gen:errors / gen:manifest) still
        # read env: the stapel-auth error registry + pyproject version pin.
        run: corepack pnpm gen:check
        env:
          AUTH_ERRORS_JSON: ${{ github.workspace }}/.errors-source/stapel-auth/docs/errors.json
          # Backend version pin → manifest backend.contract (gen:manifest).
          MANIFEST_BACKEND_PYPROJECT: ${{ github.workspace }}/.errors-source/stapel-auth/pyproject.toml

      - name: CSS guardrail (stylelint)
        # The CSS half of the guardrails (frontend-guardrails §2.2): colours in
        # .css only as var(--stapel-*) tokens. ESLint ignores *.css, so this is
        # the only gate over hand-written stylesheets (e.g. the viewer's).
        run: corepack pnpm lint:css

      - name: Lint, test, build, size
        run: corepack pnpm turbo run lint test build size

      - name: Pack purity gate (§5.1, serialized)
        # Each pair's prodBundlePurity suite shells out to `npm pack --dry-run`
        # — real I/O that flaked inside the parallel turbo `test` graph. It is
        # excluded from the per-package `test` script and runs here one package
        # at a time (pnpm --workspace-concurrency=1), contention-free.
        run: corepack pnpm run test:pack

      - name: Standalone build gate (frontend-standard §7)
        # --if-present: the gate proves every PUBLISHED package builds in
        # isolation; the private showcase-viewer app has no `build` script
        # (its Ladle build runs behind the STAPEL_INTROSPECTION gate, §5).
        run: |
          for pkg in packages/*/; do
            echo "--- standalone build: $pkg"
            (cd "$pkg" && corepack pnpm run --if-present build)
          done
