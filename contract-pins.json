{
  "$comment": "Single source of truth for the immutable refs that ci.yml and release.yml pin each sibling backend checkout to (§17 pin B). Each pinned commit CONTAINS the committed contract artifacts the gen:* drivers regenerate from: docs/schema.json, docs/flows.json, docs/errors.json, docs/capabilities.json (+ pyproject.toml for the manifest backend-version pin). Bumping a pin is a deliberate PR after the backend releases a new contract — never float main. Procedure: CONTRIBUTING.md, section 'Contract pins'. 2026-07-16: bumped to the v1-canon-sweep release tags (§60, api-versioning.md) — auth v0.5.8, notifications v0.3.8, profiles v0.3.13, billing v0.4.11, workspaces v0.4.2, calendar v0.3.4, recordings v0.3.2; all contracts now carry /<mod>/api/v1/ paths; react gen:* artifacts regenerated against these refs in the same change. 2026-08-09: the Spanish wave — auth v0.20.1, notifications v0.7.1, billing v0.6.1, workspaces v0.22.1 bumped to the releases that carry translations/errors.es.json (profiles was already there; calendar and recordings ship no locale catalogues at all and are deliberately left behind, so their pairs stay en-only rather than gaining a fabricated empty one). Each note below states what the bump drags in BESIDES the catalogue. 2026-08-23: stapel-video v0.7.0 joins the pins — the first entry added FOR a pair rather than under one: nothing in this repo read stapel-video before, and the release that gave ParticipantSpan a scope_key is the only reason @stapel/video-react exists. It is also the first pin whose gen:errors runs with ERRORS_CATALOG_DIR pointed at stapel-core (the module ships no translations/ of its own). 2026-08-23: stapel-billing v0.6.1 -> v0.8.1 — the wallet body grows lots[], holds[] and expiring_soon, which is the whole reason @stapel/billing-react's useWallet and its new /default <WalletPanel/> exist; the same span prunes the core-owned error keys out of billing's own translations/, so the ru/es bundles now depend on the stapel-core merge that was already in place.",
  "modules": {
    "stapel-core": {
      "ref": "6e273af8104fd9baa6f975876707f9db0cfc2450",
      "note": "release v0.23.1 — NOT an API-contract source: core exposes no endpoints these pairs call, and no gen:api / gen:flows / gen:manifest driver reads it. It is pinned because it owns the ~41 CROSS-CUTTING ERROR KEYS (django/translations/errors.{ru,es}.json). Until core 0.22 every module kept verbatim copies and the coverage gate took its canon from the whole in-process registry; core ships and merges them itself now, and stapel-workspaces 0.23.0 pruned its copies. scripts/gen-errors.mjs therefore merges this catalogue UNDER each module's own — the same merge the backend loader performs at runtime, so what a client bundles is what a server would have rendered. Without this checkout the merge finds nothing and every locale bundle fails 41 keys short, blaming the module's file for a sibling that was never fetched; the generator now names that cause explicitly rather than guessing. Bump this pin whenever core adds or reworks a cross-cutting key. 2026-08-21: bumped v0.23.1 -> v0.32.0 for the @stapel/forms-react enrollment. stapel-forms' registry declares the core-owned key error.503.mandate_unavailable (the mandate MAC's peer-outage refusal), which the v0.23.1 catalogue predates — gen:errors refused the es/ru bundles one key short and correctly named core's catalogue, not the module's file, as the cause. The span is a ONE-KEY delta for this repo: diffing the two refs' errors.{ru,es}.json shows exactly that key added, zero keys removed and zero texts changed, so no already-enrolled pair's locale bundle moves. Nothing else in the v0.23.1..v0.32.0 span is read from here — core exposes no endpoints these pairs call and no gen:api/gen:flows/gen:manifest driver reads it."
    },
    "stapel-auth": {
      "ref": "e644339e98329244e82af5120185bfe0f67074d3",
      "note": "release v0.20.1 — Spanish ships as a language of the library: translations/errors.es.json, 127 keys, complete over the registry by construction. Published to PyPI as 0.20.1 (the 0.20.0/0.20.1 publishes failed on a contract test importing stapel_tools.readme before stapel-tools 0.34.0 existed; re-run once that shipped, and the installed wheel was verified to carry the catalogue). Carries the pair's first errors.es.gen.ts. Also lands, from the v0.12.0..v0.20.1 span this pin skips: docs/errors.json gains error.403.privileged_account and error.403.registration_closed, and docs/schema.json widens the OTP `code` field maxLength 4 -> 8 on the seven password/TOTP/disable-otp request bodies (the backend moved to 8-digit codes; the pair's generated schema.ts follows). docs/flows.json is byte-identical across the span, so the flow machines do not move. Pin+regen bumped together."
    },
    "stapel-notifications": {
      "ref": "97fc7adadb667241ec2175b2e0cc8446040a3f9c",
      "note": "release v0.7.1 (on PyPI) — Spanish error catalogue: translations/errors.es.json, 43 keys, complete over the registry. Carries the pair's first errors.es.gen.ts. Contract delta across the span this pin skips is description-only: docs/schema.json restates the push-token register/unregister permission as `IsNotAnonymousUser` instead of `IsAuthenticated` (prose in the OpenAPI description; no path, method, field or type moved). docs/errors.json and docs/flows.json are byte-identical, so no new error key and no flow change. Pin+regen bumped together."
    },
    "stapel-profiles": {
      "ref": "b90d981e1937f0787cc1e0acbeac3c6c81131fd1",
      "note": "release v0.12.0 — the avatar/avatar_source pair invariant. A CDN reference stored with the model-default `file` tag routed to the PIL provider, which opened stapel-cdn's variant DIRECTORY as a plain file and 500'd GET /me and POST /batch; 2 of 2 profiles on the meettoday sandbox were stored that way, and the cascade (no display_name -> \"enter your name\" dialog at the meeting door -> its PATCH 500s on the same avatar) locked two people out of the product. The model now writes the source the reference implies, and a request that STATES a contradicting source is refused with the new error.400.avatar_source_mismatch. Also carries the Spanish error catalogue 0.11.0 tagged but never published (its contract gate failed). Pairs with @stapel/profiles-react's useSetAvatar, which transports the provenance instead of leaving the caller to remember it. Pin+regen bumped together. 2026-08-09: NOT moved by the Spanish wave — this pin already carries translations/errors.es.json (53 keys, complete over the registry), so profiles-react gains its errors.es.gen.ts by regeneration alone, with zero contract delta. It is the one pair whose Spanish costs no API-surface movement at all."
    },
    "stapel-billing": {
      "ref": "58a955b05915f3d346b071294f700c2edec4221a",
      "note": "release v0.8.1 — the wallet stops being a scalar. GET /wallet still answers one body, but WalletResponse now carries `lots[]` (CreditLotResponse: id, credits_remaining, credits_initial, source ∈ purchase|subscription|grant|adjustment|hold_release, expires_at nullable, created_at — server-ordered in SPEND order, expiring soonest first, non-expiring last), `holds[]` (CreditHoldResponse: the open reservations, status=held; a hold is a real withdrawal, so its credits are already OUT of `balance`) and `expiring_soon` (ExpiringCreditsResponse | null — the nearest deadline, computed by the server). All three are OPTIONAL in the schema's `required` list, so a host still on a 0.7.x server sends a body without them and the pair reads that as \"no lots to show\", not as a broken contract. THE ORDER AND THE DEADLINE ARE SERVER TRUTH: debit() walks lots `expires_at ASC NULLS LAST`, so a client that re-sorted them would be drawing a spend order the backend does not use — @stapel/billing-react re-sorts nothing. Zero paths move (the delta is three component schemas), docs/flows.json is byte-identical, and docs/errors.json grows by two codes: error.400.redirect_url_not_allowed (billing-owned, the checkout redirect allowlist) and error.503.mandate_unavailable (core-owned, already in the pinned stapel-core catalogue since the forms enrollment). LOCALES: 0.8.x prunes the core-owned keys from translations/errors.{ru,es}.json the way stapel-workspaces 0.23.0 did — 53 keys → the 13 the module actually owns — so the ru/es bundles keep their full coverage only because scripts/gen-errors.mjs merges stapel-core's catalogue underneath, the same merge the backend loader performs at runtime. Pin+regen bumped together. Superseded note, still true of the surface: v0.6.1 carried the Spanish error catalogue (53 keys, complete over the registry) and was the cleanest bump of that wave."
    },
    "stapel-workspaces": {
      "ref": "410b8812a0ac1891015b5391b562ac7f37d3771a",
      "note": "release v0.24.1 (on PyPI) — the workspace AXIS: the module can now answer \"which workspace\", say who owns one, decide who may make one, and remember who did what to a membership. Four surface deltas the pair regenerates against: (1) GET /<ws>/audit — an append-only membership history (WorkspaceAuditEvent), anchor-paginated, gated on members.view; every transition that emits a comm event now also writes a line, held by a gate that matches emitted event names against AuditAction. 0.24.1 over 0.24.0 is exactly this endpoint's contract: 0.24.0 declared no pagination_class, so the schema advertised a bare array while the view sent {items, has_next, next_anchor} — caught while regenerating THIS pair, fixed before any client shipped against the lie. (2) WorkspaceResponse.owner_display_name — best-effort from stapel-profiles, batched server-side rather than by each client, because a workspace NAME stopped identifying one (many spaces called \"Personal\", one per company, and ownership is transferable). Pickers draw it as the second line. (3) WorkspaceListResponse.can_create_workspace — the private-cloud policy (WORKSPACE_CREATE_POLICY: anyone | instance_owner) surfaced as a flag over the same helper the POST gate uses, so the button and the refusal can never disagree. (4) translations/errors.ru.json/es.json keep the 0.23.0 pruning of core-owned keys — which is why scripts/gen-errors.mjs now merges stapel-core's catalogue underneath the module's, the same merge the backend loader performs at runtime. Pin+regen bumped together. Superseded note, still true of the surface: v0.22.1 carried the Spanish catalogue and error.503.profiles_not_configured; v0.20.0 added PUT/DELETE /me/preferred-workspace + GET /'s preferred_workspace_id (WorkspaceMember.is_preferred, partial-unique per user, dies with the membership row), which is the explicit choice DEFAULT_WORKSPACE_ID had documented itself as yielding to with nowhere to be recorded — clients guessed workspaces[0] and #239 was that guess. Refusals there collapse to ONE error.404.workspace_not_found so the endpoint cannot probe which ids are real. Pairs with WorkspaceSelectionProvider / useWorkspaceSelection. Still standing from v0.19.0: the roster's two name-edit PATCHes, GET /instance, GET /'s is_guest + default_workspace_id, settings.security.capability_levels."
    },
    "stapel-calendar": {
      "ref": "a8fa34c437b62c8b1234a9b92c5ceca4215bc2d0",
      "note": "release v0.3.4 — v1 canon sweep §60: /calendar/api/v1/ paths"
    },
    "stapel-recordings": {
      "ref": "33481eee663a27c89dedf30c79e776f357c1ee04",
      "note": "current main — contract-regen catch-up"
    },
    "stapel-chat": {
      "ref": "ff12fcdd865edeb94138381f4bf19ec89ca4c2b1",
      "note": "release v0.2.2 — the ref that finally EXPORTS the socket: routing.py mounts stapel_chat.consumers.ChatConsumer at ws/chat/<uuid:conversation_id>, so the resumable protocol the consumer had always implemented (hello{last_seq} -> welcome -> replay -> replay_done -> live message frames, seq-dedup, error{resync} past REPLAY_LIMIT=500) is reachable by a host for the first time. That is why @stapel/chat-react ships two transports behind one seam instead of the polling-only v1 the storefront spec assumed (the storefront spec §3.6, corrected in §13.3). Contract artifacts read from this pin: docs/schema.json (8 paths, /chat/api/v1/), docs/errors.json (54 keys, 12 owned by stapel_chat), docs/flows.json (empty — the module annotates no @flow_step, so gen:flows emits nothing and the pair keeps the zero-flow registry shim). NO translations/ directory: chat ships English only, so gen:errors runs here with ERRORS_LOCALE_EXEMPT_OWNERS=stapel_chat and ERRORS_CATALOG_DIR pointed at stapel-core's catalogue — the 42 cross-cutting keys come from core in ru/es, the 12 chat-owned keys are authored by the pair in its own ./i18n/<locale> module until upstream localizes them (the stapel-forms/stapel_attributes precedent, one directory over)."
    },
    "stapel-forms": {
      "ref": "d66a1fb048feebebebaf98ec971fb41ab60f820b",
      "note": "release v0.2.0 — closes both upstream asks the pair filed against 0.1.0 (§11c notes 1 and 2), so two workarounds were DELETED here rather than carried. (1) docs/errors.json grows 63 -> 75 keys: the 12 stapel_attributes-owned entries (10 error.400.feature_* + the 2 description_too_* ) are now in the contract, so the pair's hand-carried English for the family is gone and the generated bundle covers the errors a respondent is most likely to see. NOTE: attributes ships English only — it has no translations/ directory at all — so those 12 keys are absent from errors.{ru,es}.json and always will be until upstream localizes them (their MODULE.md §12.6). gen-errors is therefore run for this module with ERRORS_LOCALE_EXEMPT_OWNERS=stapel_attributes; the pair keeps its own authored ru/es for the family. (2) A 14th path appears: GET /forms/api/v1/field-kinds (forms.manage, ?workspace_id= required, no admin/ prefix, no trailing slash) serves stapel-attributes' config_form() declarations as FieldKindsDTO/FieldKindDTO — so packages/forms-react/src/widgets/configForms.ts, the pinned TypeScript mirror that could drift silently, was deleted and the builder reads the registry instead. A kind the attributes registry does not carry arrives with registered:false, and one with no declaration arrives with fields:[]; both route to the builder-less path the pair already had."
    },
    "stapel-cdn": {
      "ref": "4d3b4eb51d91621571cb41e0681c4fd1ea08aca1",
      "note": "release v0.12.0 — the ref that EMITS the contract triad at all: stapel-cdn had no codegen until this release (its Makefile said so out loud), so @stapel/cdn-react could not exist before it. What is read here: docs/schema.json (8 paths under /cdn/api/v1/, 11 component schemas; Image.variants_meta carries the ladder WITH geometry — [{tier, branch, url, width, height}] — which is what lets an uploaded image be handed to @stapel/image without a second request), docs/errors.json (53 codes: 11 owned by stapel_cdn, 42 cross-cutting stapel_core) and pyproject.toml 0.12.0 -> backend.contract. docs/flows.json is an empty array — the module annotates no @flow_step, so gen:flows emits nothing and the pair keeps the zero-flow registry shim (the stapel-chat precedent, one entry up). NO translations/ directory: cdn ships English only, so gen:errors runs here with ERRORS_LOCALE_EXEMPT_OWNERS=stapel_cdn and ERRORS_CATALOG_DIR pointed at stapel-core's catalogue — the 42 cross-cutting keys come from core in ru/es, the 11 cdn-owned keys are authored by the pair in its own ./i18n/<locale> module until upstream localizes them. The same release is also the one whose views declare the byte ceilings the pair mirrors client-side (MAX_IMAGE_SIZE 20 MB, MAX_VIDEO_SIZE 100 MB, MAX_FILE_SIZE 50 MB, the extension allowlists) — see src/model/limits.ts for why the mirror is configurable rather than hardcoded."
    },
    "stapel-search": {
      "ref": "352cce979cee11936a8c822212fe87e377dd08cf",
      "note": "release v0.1.0 — the first contract this pair is generated against, and the first pin whose module ships NO usable docs/flows.json: the file is literally `{}` (an object), while scripts/gen-flows.mjs requires a JSON array and throws on anything else. stapel-search annotates no @flow_step, so an array would have been empty anyway and the pair is deliberately NOT wired into gen:flows / gen:events' flow input — it has no multi-step flow to machine (spec §8.1: flows/ only where a multi-step flow exists). Upstream ask, recorded rather than worked around: emit `[]` like every other module so the artifact set is uniform. What IS read here: docs/schema.json (5 paths, /search/api/v1/ prefixed, 9 component schemas; note info.title is empty and info.version is the placeholder 0.0.0 — the manifest's backend.contract comes from pyproject.toml, not from the schema), docs/errors.json (52 codes: 12 owned by stapel_search, 40 cross-cutting stapel_core), translations/errors.{ru,es}.json (the 12 owned keys; the ~40 core keys are merged in from the stapel-core pin, as for every other pair), and pyproject.toml 0.1.0 -> backend.contract >=0.1 <0.2. v0.1.0 is the tag; docs/ and pyproject.toml are byte-identical from the tag to the module's current main, so the pin costs the pair nothing in freshness."
    },
    "stapel-categories": {
      "ref": "6a3fde17dfc58f12dc84c1755ded300d0314f112",
      "note": "release v0.6.0 — the contract triad this pair is generated against, and the first pin whose module ships NO translations/ directory at all. What is read here: docs/schema.json (23 paths, /categories/api/v1/ prefixed, 62 component schemas), docs/errors.json (62 codes: 8 owned by stapel_categories, 12 by the embedded stapel_attributes, 42 cross-cutting stapel_core), docs/flows.json (literally `[]` — the module annotates no @flow_step, so the pair carries no flows/ layer and is not wired into gen:flows), and pyproject.toml 0.6.0 -> backend.contract >=0.6 <0.7. LOCALES: the module has no translations/ directory, so gen:errors runs here with ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_categories,stapel_attributes — the 42 cross-cutting keys come from core in ru/es, and the 20 module-and-library-owned keys are authored by the pair in its own ./i18n/<locale> module until upstream localizes them (the stapel-forms/stapel_attributes and stapel-chat precedents, both one directory over). KNOWN CONTRACT DEFECT, recorded rather than worked around twice: FeatureConfig carries discriminator {propertyName: \"type\", mapping: {\"null\": ConvertibleUnitConfig}} — a single bogus entry instead of the ten type slugs. openapi-typescript therefore strips `type` from every use site (Omit<FeatureConfig, \"type\">) and re-adds a SYNTHETIC discriminant per member, so the generated IntConfig declares type: \"IntConfig\" where the wire sends \"int\". The pair types feature configs through @stapel/attributes-react's hand-mirrored FeatureDef/FeatureConfig instead; upstream ask is to emit the ten slugs in discriminator.mapping. v0.6.0 is the tag; docs/ and pyproject.toml are byte-identical from the tag to the module's current main."
    },
    "stapel-listings": {
      "ref": "936635c602101445aecfadedd67b5d37d03453a9",
      "note": "release v0.7.0 — the release that closed this pair's ask 1: `GET /listings/api/v1/my/listings/`, the caller's OWN listings in every status, paginated in the same IDAnchorPagination envelope as my/favorites and narrowable with `?status=` (repeated parameter or one comma-separated value). Before it, `GET /listings/` answered published() and took no owner parameter, so a seller's own DRAFTS were unreachable by any call the contract offered and `src/model/mineSource.ts` named the absence instead of rendering an empty grid — that file's MY_LISTINGS_SOURCE_MISSING is now the fallback for a host that overrides the seam, not the default. The row is a new component, MyListingCard: the public ListingCard plus moderation_status (the second axis, which only an owner is owed — a published listing under re-review) and the title_draft/price_draft/images_draft twins (the list half of ask 2: the published fields are empty on a listing that has never been published, so a drafts tab built on the public card would be a column of blank rows), plus created_at/updated_at. One new error code, error.400.listing_invalid_status_filter (an unknown ?status= value is a 400 naming it, not a silent empty page), so docs/errors.json goes 63 -> 64 codes: 10 owned by stapel_listings, 12 by the embedded stapel_attributes, 42 cross-cutting stapel_core. docs/schema.json goes 12 -> 13 paths under /listings/api/v1/. docs/flows.json is still literally `[]` (the module annotates no @flow_step, so the pair keeps its zero-flow registry shim) and pyproject.toml 0.7.0 -> backend.contract. LOCALES unchanged: still no translations/ directory, so gen:errors runs with ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_listings,stapel_attributes — the new listing key is authored by the pair in its own ./i18n/<locale> module like the other nine. The 0.6.1..0.7.0 span also carries 0.6.2's two authorization fixes, which retire asks 3 and 4: PUT/PATCH now pass _get_own (this pair still writes through save-draft, but the endpoints are no longer a hole anything else that speaks the contract can walk through) and GET /{pk}/ resolves through ListingQuerySet.visible_to, so a stranger's draft 404s instead of answering 200 — the pair's isPubliclyVisible report stays, because a listing's OWNER still reads their own unpublished row there and needs to be told it is not on the shelf."
    },
    "stapel-reviews": {
      "ref": "bdf9f9183c03adb2ba1128970951146635b2086e",
      "note": "release v0.3.0 — the release that closed BOTH asks this pair's 0.2.2 pin recorded, so the two workarounds are deleted rather than carried. (1) The two reads are anonymous: ReviewListCreateView is IsAuthenticatedOrReadOnly (mirroring stapel-listings' ListingViewSet — GET open, POST still needs an author to attribute the review to) and AggregateView is AllowAny, both declaring stapel_anonymous_access = ANONYMOUS_ALLOWED and both throttled from the module's OWN settings namespace (LIST_THROTTLE 120/min, AGGREGATE_THROTTLE 300/min) rather than the project's DEFAULT_THROTTLE_RATES — stapel-search's convention. Nothing new becomes visible: both endpoints were already published-only for a non-moderator. So `signInRequired` is gone from the two READ bags of @stapel/reviews-react and survives only on the write. (2) GET /reviews now DECLARES what it returns: components/ReviewPage is the AnchorPagination envelope ({items, next_anchor, prev_anchor, has_next, has_prev, count}) and anchor / limit / direction are declared query parameters (direction with an enum of center|next|prev). Both were hand-declared in the pair's src/api/types.ts against the 0.2.2 pin and are now DELETED in favour of codegen. What is read here: docs/schema.json (4 paths under /reviews/api/v1/, 7 component schemas — ReviewPage is the new one; note both GETs now carry an empty security alternative `{}`, which is how the anonymous read shows up in the contract), docs/errors.json (51 codes: 9 owned by stapel_reviews, 42 cross-cutting stapel_core — unchanged from 0.2.2), docs/flows.json (literally `[]` — the module annotates no @flow_step, so the pair carries no flows/ layer and is not wired into gen:flows, the stapel-categories precedent) and pyproject.toml 0.3.0 -> backend.contract >=0.3 <0.4. LOCALES: still no translations/ directory, so gen:errors runs with ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_reviews — the 42 cross-cutting keys come from core in ru/es and the 9 review keys are authored by the pair in its own ./i18n/<locale> module until upstream localizes them. REMAINING CONTRACT NOTE, not a defect the pair works around: the duplicate refusal is error.400.reviews_duplicate_review — a 400 — while the module's only 409 (error.409.reviews_already_responded) is about the owner's reply, so every refusal in the pair is read by CODE and never by status. v0.3.1 (bdf9f91) is docs-only on top of v0.3.0: endpoint descriptions no longer name an internal spec; the generated schema.ts was regenerated against it."
    },
    "stapel-gdpr": {
      "ref": "5ed2c92fb0c6784ee9b7219bbaf9343c9afd6195",
      "note": "commit 98ca5cd on top of release v0.5.1 — the commit that EMITS THE CONTRACT AT ALL, and the only reason @stapel/gdpr-react can exist. stapel-gdpr had no codegen of any kind: docs/ held capabilities.json, errors.json and llms.txt, and NO schema.json, so there was nothing for gen:api / gen:manifest to read and the pair could not be generated (the stapel-cdn v0.12.0 precedent one entry up, whose Makefile said the same thing out loud). This ref adds _codegen.py / _codegen_settings.py / codegen_urls.py — the stapel-video harness verbatim in structure — mounting a single-module {gdpr + core} instance at the canonical /gdpr/api/ prefix so every emitted path reads /gdpr/api/v1/..., and commits the result. WHY A PLAIN COMMIT AND NOT A TAG: it carries no behaviour change and no version bump (pyproject stays 0.5.1, so manifest backend.contract is >=0.5 <0.6); the stapel-recordings pin two entries up is the same shape (\"current main — contract-regen catch-up\"). What is read here: docs/schema.json (13 paths / 15 operations under /gdpr/api/v1/, 16 component schemas — ClosureStatusDTO, ErasureStatusDTO with its nested ErasurePartDTO and SubprocessorObligationDTO, DsarStatusDTO, DataOwnerHealthDTO, ExportRequestDTO, ExportStatusDTO, plus the four inline request bodies), docs/errors.json (57 codes: 15 owned by stapel_gdpr, 42 cross-cutting stapel_core — byte-identical to the committed 0.5.1 file, which is how the harness proves it did not move the registry) and pyproject.toml. docs/flows.json is literally `[]` — the module annotates no @flow_step, so gen:flows emits nothing and the pair keeps the zero-flow registry shim (the chat/cdn/categories/video precedent); that is not a missing machine, since account closure's steps are a 30-day grace and a sweep task on the SERVER and a flow machine would be a client-side model of a clock it does not own. TWO CONTRACT FIXES landed with the emission, both found by building this pair against the schema and both annotation-only: GET /user/account/close/status now DECLARES the 404 it has always answered (error.404.gdpr.no_active_closure — the state almost every account is in, and the single most load-bearing refusal in the pair, folded to a null answer by useAccountClosure), and GET /dsar declares the 403 its hand-rolled staff check answers (the view is AllowAny because the POST beside it must accept an anonymous form, so drf-spectacular could not see it). Also typed: POST /internal/export/{id}/part-ready, previously exclude=True — it stays out of GdprApi (a browser holds no service credential) but is in the contract, and manifest.json records it. LOCALES: unlike video/chat/cdn, this module SHIPS translations/errors.{ru,es}.json covering all 15 keys it owns, so gen:errors runs with no ERRORS_LOCALE_EXEMPT_OWNERS and the generated ru bundle is complete over the registry — nothing in the pair is authored to paper over an upstream gap. ERRORS_LOCALES=ru pins the emitted set to the one locale the pair ships a UI bundle for; the module's Spanish catalogue is equally complete, so es is one env value plus one authored bundle away (recorded rather than half-done). THE REFUSAL VOCABULARY THIS PAIR IS SHAPED BY, all declared in this ref: three 404s that mean three different things (no_active_closure = your account is fine; export_not_found = you never asked for an archive; erasure_not_found = a real miss), two 409s that are a no-op and a legal refusal (closure_already_pending vs legal_hold), and two 410s that are opposite advice at one status (download_consumed vs download_expired). Every predicate in the pair reads the CODE and never the status, because a status check would confuse all three pairs. re-pinned at v0.5.2 (triad release)"
    },
    "stapel-video": {
      "ref": "9441461eb445eacab3ccb2fb76c524068605e032",
      "note": "release v0.7.0 — the release that gave the presence meter a TENANT, and the only reason @stapel/video-react exists at all. Until 0.7.0 ParticipantSpan carried a room and a person and no partition, so \"how long did the people in THIS workspace talk\" could only be answered by a host joining the span table to its own rooms and re-implementing the union arithmetic beside the table that already owns it — a second answer to a billable number that nobody reconciles until a customer disputes it. 0.7.0 adds ParticipantSpan.scope_key (nullable, stamped from the JOIN GRANT because that is the only moment the tenant is in the process), video.presence.usage_rollup / usage_rollup_by_month over the same union code path as presence.aggregate, and the read this pair is built on. What is read here: docs/schema.json (8 paths under /video/api/v1/, 11 component schemas — ScopeUsageResponse/ScopeUsageMonth/ScopeUsageRow are the three this pair generates against; note ScopeUsageResponse.months and ScopeUsageMonth.users are NOT in `required`, so the generated types make them optional and src/model/usage.ts is the one place allowed to decide that a missing months array is \"no months\" rather than a crash), docs/errors.json (51 codes: 9 owned by stapel_video, 42 cross-cutting stapel_core) and pyproject.toml 0.7.0 -> backend.contract >=0.7 <0.8. docs/flows.json is literally `[]` — the module annotates no @flow_step, so gen:flows emits nothing and the pair keeps the zero-flow registry shim (the stapel-chat / stapel-cdn / stapel-categories precedent). LOCALES: no translations/ directory at all, so gen:errors runs with ERRORS_LOCALES=ru, ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_video — the 42 cross-cutting keys come from core in ru, and the 9 video keys are authored by the pair in its own ./i18n/ru module until upstream localizes them. THE TWO REFUSALS THIS PAIR IS SHAPED BY, both declared in this ref: error.404.video_scope_not_found is deliberately UNIFORM over three different situations — the scope does not exist, the scope has no calls, and the caller holds no USAGE_MANDATE in it — because a 403 would confirm that a guessed tenant id is real; the pair therefore renders it as an explained \"not available for this workspace\", never as an empty table, and never guesses which of the three it was. error.400.video_invalid_usage_period is the answer to a malformed `month` or an out-of-range `months` (default 6, max 36), which is why the hook takes the two request shapes as a discriminated union instead of a bag of loose strings. Month buckets are cut at LOCAL midnight in the requested `tz`, so a DST month is genuinely 743 or 745 hours and the pair never re-derives a boundary client-side — period_start/period_end come off the wire."
    }
  }
}
