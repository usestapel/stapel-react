{
  "$comment": "Single source of truth for the immutable refs that ci.yml and release.yml pin each sibling backend checkout to (§17 pin B). Each pinned commit CONTAINS the committed contract artifacts the gen:* drivers regenerate from: docs/schema.json, docs/flows.json, docs/errors.json, docs/capabilities.json (+ pyproject.toml for the manifest backend-version pin). Bumping a pin is a deliberate PR after the backend releases a new contract — never float main. Procedure: CONTRIBUTING.md, section 'Contract pins'. 2026-07-16: bumped to the v1-canon-sweep release tags (§60, api-versioning.md) — auth v0.5.8, notifications v0.3.8, profiles v0.3.13, billing v0.4.11, workspaces v0.4.2, calendar v0.3.4, recordings v0.3.2; all contracts now carry /<mod>/api/v1/ paths; react gen:* artifacts regenerated against these refs in the same change. 2026-08-09: the Spanish wave — auth v0.20.1, notifications v0.7.1, billing v0.6.1, workspaces v0.22.1 bumped to the releases that carry translations/errors.es.json (profiles was already there; calendar and recordings ship no locale catalogues at all and are deliberately left behind, so their pairs stay en-only rather than gaining a fabricated empty one). Each note below states what the bump drags in BESIDES the catalogue. 2026-08-23: stapel-video v0.7.0 joins the pins — the first entry added FOR a pair rather than under one: nothing in this repo read stapel-video before, and the release that gave ParticipantSpan a scope_key is the only reason @stapel/video-react exists. It is also the first pin whose gen:errors runs with ERRORS_CATALOG_DIR pointed at stapel-core (the module ships no translations/ of its own). 2026-08-23: stapel-billing v0.6.1 -> v0.8.1 — the wallet body grows lots[], holds[] and expiring_soon, which is the whole reason @stapel/billing-react's useWallet and its new /default <WalletPanel/> exist; the same span prunes the core-owned error keys out of billing's own translations/, so the ru/es bundles now depend on the stapel-core merge that was already in place.",
  "modules": {
    "stapel-core": {
      "ref": "6e1e3169a123efb05b8bad7fd0212b331cbfc985",
      "note": "release v0.47.0 — 2026-08-26: core IS a contract source after all: the shared error catalogue (django/translations/errors.*.json) every pair merges is read from this pin; a stale core pin loses every newer cross-cutting code in ru/es. release v0.23.1 — NOT an API-contract source: core exposes no endpoints these pairs call, and no gen:api / gen:flows / gen:manifest driver reads it. It is pinned because it owns the ~41 CROSS-CUTTING ERROR KEYS (django/translations/errors.{ru,es}.json). Until core 0.22 every module kept verbatim copies and the coverage gate took its canon from the whole in-process registry; core ships and merges t"
    },
    "stapel-auth": {
      "ref": "23a81693ef7b60a52aba0de4a4101b4259b05fce",
      "note": "release v0.28.1 — 2026-08-26 wave D regen. release v0.27.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.25.2 — the OTP width stops being two numbers. The backend used to compute the code length twice: generation read MOCK_OTP_CODE, GET /capabilities/ reported OTP_LENGTH. A stand with mocked OTP delivery therefore issued a four-character code while telling this pair, through otp.email_code_length/phone_code_length, that codes are six digits — and <OtpPanel/>, which correctly reads the contract instead of hardcoding, drew six boxes nobody could fill. 0.25.2 makes otp.services.issued_code_length(channel) the single source both the issuing path and the contract consume, so a mocked channel "
    },
    "stapel-notifications": {
      "ref": "8e2ad21d195349fb2e943adf53cc62d1019dce7f",
      "note": "release v0.18.0 — 2026-08-26: read_at / unread_count / POST feed/read/ / notification.read signal. release v0.17.3 — 2026-08-26: the owner-scoped error registry (45 keys) the pair bundles. release v0.17.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.7.1 (on PyPI) — Spanish error catalogue: translations/errors.es.json, 43 keys, complete over the registry. Carries the pair's first errors.es.gen.ts. Contract delta across the span this pin skips is de"
    },
    "stapel-profiles": {
      "ref": "3cd63f6dc774a4f70c3cfcda798cf10c044aaffa",
      "note": "release v0.16.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.12.0 — the avatar/avatar_source pair invariant. A CDN reference stored with the model-default `file` tag routed to the PIL provider, which opened stapel-cdn's variant DIRECTORY as a plain file and 500'd GET /me and POST /batch; 2 of 2 profiles on the meettoday sandbox were stored that way, and the cascade (no display_name -> \"enter your name\" dialog at the meeting door -> its PATCH 500s on the same avatar) locked two people out of the product. The model now writes the source the reference implies, and a request that STATES a contradicting source is refused with the new error.400.avatar_source_mismatch. Also carries the Spanish error catalogue 0.11.0 tagged but never published (its contract gate failed). Pairs with @stapel/profiles-react's useSetAvatar, which transports the provena"
    },
    "stapel-billing": {
      "ref": "a2d74165a675d40e782ff94c67d9e42632d544df",
      "note": "release v0.11.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.8.1 — the wallet stops being a scalar. GET /wallet still answers one body, but WalletResponse now carries `lots[]` (CreditLotResponse: id, credits_remaining, credits_initial, source ∈ purchase|subscription|grant|adjustment|hold_release, expires_at nullable, created_at — server-ordered in SPEND order, expiring soonest first, non-expiring last), `holds[]` (CreditHoldResponse: the open reservations, status=held; a hold is a real withdrawal, so its credits are already OUT of `balance`) and `expiring_soon` (ExpiringCreditsResponse | null — the nearest deadline, computed by the server). All three are OPTIONAL in the schema's `required` list, so a host still on a 0.7.x server sends a body without them and the pair reads that as \"no lots to show\", not as a broken contract. THE ORDER AND T"
    },
    "stapel-workspaces": {
      "ref": "f4c1b0b4b4d167ede5ae4483d16b7255a111c258",
      "note": "release v0.30.0 — 2026-08-26 wave D regen. release v0.29.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.24.1 (on PyPI) — the workspace AXIS: the module can now answer \"which workspace\", say who owns one, decide who may make one, and remember who did what to a membership. Four surface deltas the pair regenerates against: (1) GET /<ws>/audit — an append-only membership history (WorkspaceAuditEvent), anchor-paginated, gated on members.view; every transition that emits a comm event now also writes a line, held by a gate that matches emitted event names against AuditAction. 0.24.1 over 0.24.0 is exactly this endpoint's contract: 0.24.0 declared no pagination_class, so the schema advertised a"
    },
    "stapel-calendar": {
      "ref": "876ceb7581cb46abcc8370b0d2a19f4a44e9b93f",
      "note": "release v0.6.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.3.4 — v1 canon sweep §60: /calendar/api/v1/ paths"
    },
    "stapel-recordings": {
      "ref": "d460256b5f51a58ea54091c0602f792eae4521b7",
      "note": "release v0.20.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. current main — contract-regen catch-up"
    },
    "stapel-chat": {
      "ref": "808128e48c3771326175e6fcc403ce6fa8073c1b",
      "note": "release v0.6.0 — 2026-08-26: chat-react regenerated against the released tag (0.6.1 tagged, not on PyPI yet). release v0.2.2 — the ref that finally EXPORTS the socket: routing.py mounts stapel_chat.consumers.ChatConsumer at ws/chat/<uuid:conversation_id>, so the resumable protocol the consumer had always implemented (hello{last_seq} -> welcome -> replay -> replay_done -> live message frames, seq-dedup, error{resync} past REPLAY_LIMIT=500) is reachable by a host for the first time. That is why @stapel/chat-react ships two transports behind one seam instead of the polling-only v1 the storefront spec assum"
    },
    "stapel-forms": {
      "ref": "6345c53bccf883848a4f6ccc9d87cefb20a5ba53",
      "note": "release v0.4.0 — 2026-08-26: forms-react regenerated (forms.responses.manage capability). release v0.2.0 — closes both upstream asks the pair filed against 0.1.0 (§11c notes 1 and 2), so two workarounds were DELETED here rather than carried. (1) docs/errors.json grows 63 -> 75 keys: the 12 stapel_attributes-owned entries (10 error.400.feature_* + the 2 description_too_* ) are now in the contract, so the pair's hand-carried English for the family is gone and the generated bundle covers the errors a respondent is most likely to see. NOTE: attributes ships English only — it has no translations/ directory at all — so those 12 keys are absent from errors.{ru,es}.json and always will be "
    },
    "stapel-cdn": {
      "ref": "67314898d4979eac7a34bc59868ec6bc88dc8f9e",
      "note": "release v0.17.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.12.0 — the ref that EMITS the contract triad at all: stapel-cdn had no codegen until this release (its Makefile said so out loud), so @stapel/cdn-react could not exist before it. What is read here: docs/schema.json (8 paths under /cdn/api/v1/, 11 component schemas; Image.variants_meta carries the ladder WITH geometry — [{tier, branch, url, width, height}] — which is what lets an uploaded image be handed to @stapel/image without a second request), docs/errors.json (53 codes: 11 owned by stapel_cdn, 42 cross-cutting stapel_core) and pyproject.toml 0.12.0 -> backend.contract. docs/flows.json is an empty array — the module annotates no @flow_step, so gen:flows emits nothing and the pair keeps the zero-flow registry shim (the stapel-chat precedent, one entry up). NO translations/ direc"
    },
    "stapel-search": {
      "ref": "79d227b83c8dc35133d6f0955bc3834d022d7875",
      "note": "release v0.2.2 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.2.0 — the count contract. The 0.1.0 answer could say `count: 0` next to a non-empty items[] (the Postgres typo fallback answered from the trigram arm while the total kept counting the tsquery arm that had just found nothing), and this pair printed it as «Примерно 0 объявлений» over the visible cards. 0.2.0 makes `count` NULLABLE, adds `count_is_lower_bound: bool` (a floor — render \"N+\"), and redefines `exact_total` as a property of THIS answer rather than of the engine class, with `degraded: [\"exact_total\"]` following it. That is the whole reason for the bump: @stapel/search-react 0.4.0 renders the three states (exact / floor / unknown) and stops raising a degradation banner for an `exact_total`-only degradation, which is a count nuance and not a failed search. What is read here is"
    },
    "stapel-categories": {
      "ref": "24014e21b5e14aa9b3b048c49488abffc88ca8ba",
      "note": "release v0.6.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.6.0 — the contract triad this pair is generated against, and the first pin whose module ships NO translations/ directory at all. What is read here: docs/schema.json (23 paths, /categories/api/v1/ prefixed, 62 component schemas), docs/errors.json (62 codes: 8 owned by stapel_categories, 12 by the embedded stapel_attributes, 42 cross-cutting stapel_core), docs/flows.json (literally `[]` — the module annotates no @flow_step, so the pair carries no flows/ layer and is not wired into gen:flows), and pyproject.toml 0.6.0 -> backend.contract >=0.6 <0.7. LOCALES: the module has no translations/ directory, so gen:errors runs here with ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_categories,stapel_attributes — the 42 cross-cutting keys come fro"
    },
    "stapel-listings": {
      "ref": "4cb3e74aadc905b4e53964cadb48a3075d7176d9",
      "note": "release v0.7.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.7.0 — the release that closed this pair's ask 1: `GET /listings/api/v1/my/listings/`, the caller's OWN listings in every status, paginated in the same IDAnchorPagination envelope as my/favorites and narrowable with `?status=` (repeated parameter or one comma-separated value). Before it, `GET /listings/` answered published() and took no owner parameter, so a seller's own DRAFTS were unreachable by any call the contract offered and `src/model/mineSource.ts` named the absence instead of rendering an empty grid — that file's MY_LISTINGS_SOURCE_MISSING is now the fallback for a host that overrides the seam, not the default. The row is a new component, MyListingCard: the public ListingCard plus moderation_status (the second axis, which only an owner is owed — a published listing under re"
    },
    "stapel-reviews": {
      "ref": "bdf9f9183c03adb2ba1128970951146635b2086e",
      "note": "release v0.3.0 — the release that closed BOTH asks this pair's 0.2.2 pin recorded, so the two workarounds are deleted rather than carried. (1) The two reads are anonymous: ReviewListCreateView is IsAuthenticatedOrReadOnly (mirroring stapel-listings' ListingViewSet — GET open, POST still needs an author to attribute the review to) and AggregateView is AllowAny, both declaring stapel_anonymous_access = ANONYMOUS_ALLOWED and both throttled from the module's OWN settings namespace (LIST_THROTTLE 120/min, AGGREGATE_THROTTLE 300/min) rather than the project's DEFAULT_THROTTLE_RATES — stapel-search's convention. Nothing new becomes visible: both endpoints were already published-only for a non-moderator. So `signInRequired` is gone from the two READ bags of @stapel/reviews-react and survives only on the write. (2) GET /reviews now DECLARES what it returns: components/ReviewPage is the AnchorPagination envelope ({items, next_anchor, prev_anchor, has_next, has_prev, count}) and anchor / limit / direction are declared query parameters (direction with an enum of center|next|prev). Both were hand-declared in the pair's src/api/types.ts against the 0.2.2 pin and are now DELETED in favour of codegen. What is read here: docs/schema.json (4 paths under /reviews/api/v1/, 7 component schemas — ReviewPage is the new one; note both GETs now carry an empty security alternative `{}`, which is how the anonymous read shows up in the contract), docs/errors.json (51 codes: 9 owned by stapel_reviews, 42 cross-cutting stapel_core — unchanged from 0.2.2), docs/flows.json (literally `[]` — the module annotates no @flow_step, so the pair carries no flows/ layer and is not wired into gen:flows, the stapel-categories precedent) and pyproject.toml 0.3.0 -> backend.contract >=0.3 <0.4. LOCALES: still no translations/ directory, so gen:errors runs with ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_reviews — the 42 cross-cutting keys come from core in ru/es and the 9 review keys are authored by the pair in its own ./i18n/<locale> module until upstream localizes them. REMAINING CONTRACT NOTE, not a defect the pair works around: the duplicate refusal is error.400.reviews_duplicate_review — a 400 — while the module's only 409 (error.409.reviews_already_responded) is about the owner's reply, so every refusal in the pair is read by CODE and never by status. v0.3.1 (bdf9f91) is docs-only on top of v0.3.0: endpoint descriptions no longer name an internal spec; the generated schema.ts was regenerated against it."
    },
    "stapel-gdpr": {
      "ref": "9913a080509f3b2fede2504e42e7f9edcf332891",
      "note": "release v0.5.3 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. commit 98ca5cd on top of release v0.5.1 — the commit that EMITS THE CONTRACT AT ALL, and the only reason @stapel/gdpr-react can exist. stapel-gdpr had no codegen of any kind: docs/ held capabilities.json, errors.json and llms.txt, and NO schema.json, so there was nothing for gen:api / gen:manifest to read and the pair could not be generated (the stapel-cdn v0.12.0 precedent one entry up, whose Makefile said the same thing out loud). This ref adds _codegen.py / _codegen_settings.py / codegen_urls.py — the stapel-video harness verbatim in structure — mounting a single-module {gdpr + core} instance at the canonical /gdpr/api/ prefix so every emitted path reads /gdpr/api/v1/..., and commits the result. WHY A PLAIN COMMIT AND NOT A TAG: it carries no behaviour change and no version bump (pyproject"
    },
    "stapel-video": {
      "ref": "37f0a5e59ed8187691bfb54d27e3f4c381d6646f",
      "note": "release v0.9.0 — 2026-08-26 wave D regen. release v0.8.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. release v0.7.0 — the release that gave the presence meter a TENANT, and the only reason @stapel/video-react exists at all. Until 0.7.0 ParticipantSpan carried a room and a person and no partition, so \"how long did the people in THIS workspace talk\" could only be answered by a host joining the span table to its own rooms and re-implementing the union arithmetic beside the table that already owns it — a second answer to a billable number that nobody reconciles until a customer disputes it. 0.7.0 adds ParticipantSpan.scope_key (nullable, stamped from the JOIN GRANT because that is the only moment th"
    },
    "stapel-geo": {
      "ref": "108f792e86059d2d8c520558b21ce77fa91955ec",
      "note": "2026-08-26: geo-react ships ru+es locale bundles. release 0.4.0 - the release that made a location a HUMAN thing and the only reason @stapel/geo-react can exist. Until 0.4.0 the module shipped coordinates and called it a feature, which is how a live listing composer ended up with two raw fields named latitude and longitude; 0.4.0 adds GET map/config (public - the tile layer, its ATTRIBUTION OBLIGATION, the search discipline, and the endpoint table a client is asked to read its four other paths from) and GET geocoding/resolve (one round trip: display line, address components, alternatives, geohash, opt-in nearest Location rows), which are the two calls the default picker is built on. What is read here: docs/schema.json (12 paths under /geo/api/v1/, 12 component schemas - MapConfig, TileLayer, GeocodeResponse/Feature/Geometry/Properties and PlaceResolution/PlaceSummary are the ones this pair generates against), docs/errors.json (50 codes: 8 owned by stapel_geo, 42 cross-cutting stapel_core) and pyproject.toml 0.4.0 -> backend.contract >=0.4 <0.5. docs/flows.json carries two flows (geo.pick_location, geo.geocode_address); the pair drives the pick from a bag rather than a flow machine, because the steps are a finger on a map and a browser permission prompt, neither of which the server sequences. LOCALES: no translations/ directory, so gen:errors runs with ERRORS_LOCALES=ru, ERRORS_CATALOG_DIR pointed at stapel-core's catalogue and ERRORS_LOCALE_EXEMPT_OWNERS=stapel_geo - the 42 cross-cutting keys come from core in ru, the 8 geo keys are authored by the pair in its own ./i18n/ru until upstream localizes them. THE REFUSALS THIS PAIR IS SHAPED BY: 401/403 on the four geocoding verbs is the DEFAULT permission (GEOCODER_PERMISSIONS), so 'geocoding is not available to me' is a first-class state and NOT an error - the map renders and the pin drops for an anonymous visitor, only the address is missing; 429 is the server asking for quiet and keeps the last good suggestions on screen rather than replacing them with a red box; error.502.geocoder_unavailable is the one retryable failure, where every 400 is not; and a SUCCESSFUL call with zero features is an empty state (the middle of a lake has coordinates too), which is why nothing in the pair folds an empty answer into a failure."
    },
    "stapel-moderation": {
      "ref": "7585d13073717fe7bf24ed3813353479d18e5052",
      "note": "release v0.3.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. "
    },
    "stapel-attributes": {
      "ref": "a45bfa1e2e9869bdf4d0e2f43d439581d39ced70",
      "note": "release v0.4.7 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. "
    },
    "stapel-currencies": {
      "ref": "c864c4067059d0f034b33f365cf8d33a7639c7d7",
      "note": "release v0.1.9 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. "
    },
    "stapel-tasks": {
      "ref": "d7baf7cb350045efdb1213855fd57b6392fbbb8d",
      "note": "release v0.3.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. "
    },
    "stapel-translate": {
      "ref": "a34b274daa75f4805277ce3622707f3ac589d68d",
      "note": "release v0.7.0 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. "
    },
    "stapel-webhooks": {
      "ref": "b47753ddbdadada83cc25f02a6a08f6c3bc0be1f",
      "note": "release v0.1.1 — 2026-08-24 pair wave: the pair regenerated schema/errors against this contract. "
    },
    "stapel-docs": {
      "ref": "5610271a7aee08ad13e39fdcc1f17de9849dc496",
      "note": "release v0.3.0 — docs-react enrolled in the gen chains 2026-08-24; the pair reads docs/schema.json, docs/flows.json, docs/errors.json."
    }
  }
}
