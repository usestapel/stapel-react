{
  "$comment": "Single source of truth for the immutable refs that ci.yml and release.yml pin each sibling backend checkout to (§17 pin B). Each pinned commit CONTAINS the committed contract artifacts the gen:* drivers regenerate from: docs/schema.json, docs/flows.json, docs/errors.json, docs/capabilities.json (+ pyproject.toml for the manifest backend-version pin). Bumping a pin is a deliberate PR after the backend releases a new contract — never float main. Procedure: CONTRIBUTING.md, section 'Contract pins'. 2026-07-16: bumped to the v1-canon-sweep release tags (§60, api-versioning.md) — auth v0.5.8, notifications v0.3.8, profiles v0.3.13, billing v0.4.11, workspaces v0.4.2, calendar v0.3.4, recordings v0.3.2; all contracts now carry /<mod>/api/v1/ paths; react gen:* artifacts regenerated against these refs in the same change. 2026-08-09: the Spanish wave — auth v0.20.1, notifications v0.7.1, billing v0.6.1, workspaces v0.22.1 bumped to the releases that carry translations/errors.es.json (profiles was already there; calendar and recordings ship no locale catalogues at all and are deliberately left behind, so their pairs stay en-only rather than gaining a fabricated empty one). Each note below states what the bump drags in BESIDES the catalogue.",
  "modules": {
    "stapel-core": {
      "ref": "6e273af8104fd9baa6f975876707f9db0cfc2450",
      "note": "release v0.23.1 — NOT an API-contract source: core exposes no endpoints these pairs call, and no gen:api / gen:flows / gen:manifest driver reads it. It is pinned because it owns the ~41 CROSS-CUTTING ERROR KEYS (django/translations/errors.{ru,es}.json). Until core 0.22 every module kept verbatim copies and the coverage gate took its canon from the whole in-process registry; core ships and merges them itself now, and stapel-workspaces 0.23.0 pruned its copies. scripts/gen-errors.mjs therefore merges this catalogue UNDER each module's own — the same merge the backend loader performs at runtime, so what a client bundles is what a server would have rendered. Without this checkout the merge finds nothing and every locale bundle fails 41 keys short, blaming the module's file for a sibling that was never fetched; the generator now names that cause explicitly rather than guessing. Bump this pin whenever core adds or reworks a cross-cutting key. 2026-08-21: bumped v0.23.1 -> v0.32.0 for the @stapel/forms-react enrollment. stapel-forms' registry declares the core-owned key error.503.mandate_unavailable (the mandate MAC's peer-outage refusal), which the v0.23.1 catalogue predates — gen:errors refused the es/ru bundles one key short and correctly named core's catalogue, not the module's file, as the cause. The span is a ONE-KEY delta for this repo: diffing the two refs' errors.{ru,es}.json shows exactly that key added, zero keys removed and zero texts changed, so no already-enrolled pair's locale bundle moves. Nothing else in the v0.23.1..v0.32.0 span is read from here — core exposes no endpoints these pairs call and no gen:api/gen:flows/gen:manifest driver reads it."
    },
    "stapel-auth": {
      "ref": "e644339e98329244e82af5120185bfe0f67074d3",
      "note": "release v0.20.1 — Spanish ships as a language of the library: translations/errors.es.json, 127 keys, complete over the registry by construction. Published to PyPI as 0.20.1 (the 0.20.0/0.20.1 publishes failed on a contract test importing stapel_tools.readme before stapel-tools 0.34.0 existed; re-run once that shipped, and the installed wheel was verified to carry the catalogue). Carries the pair's first errors.es.gen.ts. Also lands, from the v0.12.0..v0.20.1 span this pin skips: docs/errors.json gains error.403.privileged_account and error.403.registration_closed, and docs/schema.json widens the OTP `code` field maxLength 4 -> 8 on the seven password/TOTP/disable-otp request bodies (the backend moved to 8-digit codes; the pair's generated schema.ts follows). docs/flows.json is byte-identical across the span, so the flow machines do not move. Pin+regen bumped together."
    },
    "stapel-notifications": {
      "ref": "97fc7adadb667241ec2175b2e0cc8446040a3f9c",
      "note": "release v0.7.1 (on PyPI) — Spanish error catalogue: translations/errors.es.json, 43 keys, complete over the registry. Carries the pair's first errors.es.gen.ts. Contract delta across the span this pin skips is description-only: docs/schema.json restates the push-token register/unregister permission as `IsNotAnonymousUser` instead of `IsAuthenticated` (prose in the OpenAPI description; no path, method, field or type moved). docs/errors.json and docs/flows.json are byte-identical, so no new error key and no flow change. Pin+regen bumped together."
    },
    "stapel-profiles": {
      "ref": "b90d981e1937f0787cc1e0acbeac3c6c81131fd1",
      "note": "release v0.12.0 — the avatar/avatar_source pair invariant. A CDN reference stored with the model-default `file` tag routed to the PIL provider, which opened stapel-cdn's variant DIRECTORY as a plain file and 500'd GET /me and POST /batch; 2 of 2 profiles on the meettoday sandbox were stored that way, and the cascade (no display_name -> \"enter your name\" dialog at the meeting door -> its PATCH 500s on the same avatar) locked two people out of the product. The model now writes the source the reference implies, and a request that STATES a contradicting source is refused with the new error.400.avatar_source_mismatch. Also carries the Spanish error catalogue 0.11.0 tagged but never published (its contract gate failed). Pairs with @stapel/profiles-react's useSetAvatar, which transports the provenance instead of leaving the caller to remember it. Pin+regen bumped together. 2026-08-09: NOT moved by the Spanish wave — this pin already carries translations/errors.es.json (53 keys, complete over the registry), so profiles-react gains its errors.es.gen.ts by regeneration alone, with zero contract delta. It is the one pair whose Spanish costs no API-surface movement at all."
    },
    "stapel-billing": {
      "ref": "98eb23688b0e094bdd3cadaf92226401b04f8714",
      "note": "release v0.6.1 (on PyPI) — Spanish error catalogue: translations/errors.es.json, 53 keys, complete over the registry. Carries the pair's first errors.es.gen.ts. The cleanest bump of the wave: docs/schema.json, docs/errors.json and docs/flows.json are all byte-identical across the span this pin skips, so nothing but the catalogue and the backend-version pin in the manifest moves. Pin+regen bumped together."
    },
    "stapel-workspaces": {
      "ref": "410b8812a0ac1891015b5391b562ac7f37d3771a",
      "note": "release v0.24.1 (on PyPI) — the workspace AXIS: the module can now answer \"which workspace\", say who owns one, decide who may make one, and remember who did what to a membership. Four surface deltas the pair regenerates against: (1) GET /<ws>/audit — an append-only membership history (WorkspaceAuditEvent), anchor-paginated, gated on members.view; every transition that emits a comm event now also writes a line, held by a gate that matches emitted event names against AuditAction. 0.24.1 over 0.24.0 is exactly this endpoint's contract: 0.24.0 declared no pagination_class, so the schema advertised a bare array while the view sent {items, has_next, next_anchor} — caught while regenerating THIS pair, fixed before any client shipped against the lie. (2) WorkspaceResponse.owner_display_name — best-effort from stapel-profiles, batched server-side rather than by each client, because a workspace NAME stopped identifying one (many spaces called \"Personal\", one per company, and ownership is transferable). Pickers draw it as the second line. (3) WorkspaceListResponse.can_create_workspace — the private-cloud policy (WORKSPACE_CREATE_POLICY: anyone | instance_owner) surfaced as a flag over the same helper the POST gate uses, so the button and the refusal can never disagree. (4) translations/errors.ru.json/es.json keep the 0.23.0 pruning of core-owned keys — which is why scripts/gen-errors.mjs now merges stapel-core's catalogue underneath the module's, the same merge the backend loader performs at runtime. Pin+regen bumped together. Superseded note, still true of the surface: v0.22.1 carried the Spanish catalogue and error.503.profiles_not_configured; v0.20.0 added PUT/DELETE /me/preferred-workspace + GET /'s preferred_workspace_id (WorkspaceMember.is_preferred, partial-unique per user, dies with the membership row), which is the explicit choice DEFAULT_WORKSPACE_ID had documented itself as yielding to with nowhere to be recorded — clients guessed workspaces[0] and #239 was that guess. Refusals there collapse to ONE error.404.workspace_not_found so the endpoint cannot probe which ids are real. Pairs with WorkspaceSelectionProvider / useWorkspaceSelection. Still standing from v0.19.0: the roster's two name-edit PATCHes, GET /instance, GET /'s is_guest + default_workspace_id, settings.security.capability_levels."
    },
    "stapel-calendar": {
      "ref": "a8fa34c437b62c8b1234a9b92c5ceca4215bc2d0",
      "note": "release v0.3.4 — v1 canon sweep §60: /calendar/api/v1/ paths"
    },
    "stapel-recordings": {
      "ref": "33481eee663a27c89dedf30c79e776f357c1ee04",
      "note": "current main — contract-regen catch-up"
    },
    "stapel-forms": {
      "ref": "d66a1fb048feebebebaf98ec971fb41ab60f820b",
      "note": "release v0.2.0 — closes both upstream asks the pair filed against 0.1.0 (§11c notes 1 and 2), so two workarounds were DELETED here rather than carried. (1) docs/errors.json grows 63 -> 75 keys: the 12 stapel_attributes-owned entries (10 error.400.feature_* + the 2 description_too_* ) are now in the contract, so the pair's hand-carried English for the family is gone and the generated bundle covers the errors a respondent is most likely to see. NOTE: attributes ships English only — it has no translations/ directory at all — so those 12 keys are absent from errors.{ru,es}.json and always will be until upstream localizes them (their MODULE.md §12.6). gen-errors is therefore run for this module with ERRORS_LOCALE_EXEMPT_OWNERS=stapel_attributes; the pair keeps its own authored ru/es for the family. (2) A 14th path appears: GET /forms/api/v1/field-kinds (forms.manage, ?workspace_id= required, no admin/ prefix, no trailing slash) serves stapel-attributes' config_form() declarations as FieldKindsDTO/FieldKindDTO — so packages/forms-react/src/widgets/configForms.ts, the pinned TypeScript mirror that could drift silently, was deleted and the builder reads the registry instead. A kind the attributes registry does not carry arrives with registered:false, and one with no declaration arrives with fields:[]; both route to the builder-less path the pair already had."
    }
  }
}
